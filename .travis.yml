language: php

node_js: '8'
cache: yarn

install:
  - yarn install

php:
  - 7.0

env:
  - DB=mysql

addons:
  sonarcloud:
    organization: "utopszkij-github"
      token:  
        secure: 013traMarika= # encrypted value of your token

script: 
  - yarn build
  - tests/test.sh
  - sonar-scanner

